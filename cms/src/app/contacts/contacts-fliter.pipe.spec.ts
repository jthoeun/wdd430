import { ContactsFliterPipe } from './contacts-fliter.pipe';

describe('ContactsFliterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactsFliterPipe();
    expect(pipe).toBeTruthy();
  });
});
